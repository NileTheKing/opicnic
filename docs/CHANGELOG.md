# Changelog

완료된 기능/변경 이력. 과거 항목은 안 고친다 — 나중에 방향이 바뀌어도 여기 기록은 "그 시점에 그렇게 했다"는 사실 그대로 남긴다. 지금 뭘 만들었는지 현재 상태가 궁금하면 README.md/AGENTS.md/DOMAIN.md를 먼저 보고, 여기는 "언제 뭘 했는지" 타임라인이 필요할 때 참고.

- OPIc 출제 알고리즘 (Topic & Difficulty 기반 랜덤 콤보) 구현
- Groq Whisper 기반 STT 연동 (초기 Gemini → Groq 마이그레이션)
- Groq llama-3.3-70b 기반 피드백 엔진 구축 (Spring AI OpenAI starter 호환), 이후 llama-4-scout로 교체
- Java 21 Virtual Threads + StructuredTaskScope 병렬 처리 (per-subtask 재시도 포함)
- Bucket4j Rate Limiting (사용자 ID 기반, 10회/시간)
- FeedbackResult DB 저장 (Member 연관, 성공한 피드백만 저장)
- 피드백 실패 시 부분 결과 반환 (전체 오류 대신 실패 카드 표시)
- Oracle Cloud + ~~DuckDNS + Let's Encrypt~~ **Cloudflare + Origin Certificate** + Nginx 배포 완료 (2026-06-04). DuckDNS는 Let's Encrypt SERVFAIL 반복으로 포기, Cloudflare로 전환
- dev/prod 프로파일 분리 + 모바일 반응형 (2026-06-06)
- `restoreQuestionsForIndexes` ConcurrentHashMap 캐싱 + `@Transactional(readOnly=true)` 제거 → start p95 16.69s→20ms, answers p95 20.5s→3.73s (2026-06-11)
- Feedback 점수 필드 6개 + 한국어 전용 프롬프트 (2026-06-11)
- 돌발 주제 23개 전용 풀 분리 + QuestionSet 데이터 완성 + MockExamService 정합화 (2026-06-12)
- 주제탐색 카테고리화 UI + 토픽 토글 즉시 반영 (2026-06-12)
- 코칭 리포트 자유텍스트 → 태그 기반 아키텍처 재설계 (자기모순 리포트 문제 해결) (2026-07)
- Study board(스터디 게시판) 기능 제거 — 사용되지 않던 기능 (2026-07)
- Admin 질문세트 CRUD, PracticeAttempt 엔드포인트를 REST 원칙에 맞게 정비 (리소스 경로, 공통 에러 응답, 검증) (2026-07)
- 유형별 연습 모드(`/practice/type`)가 이미 구현·동작 중이었다는 게 뒤늦게 확인됨 — `docs/hold.md`/`docs/backlog.md`에 "미구현"으로 잘못 남아있던 걸 정정 (2026-07-15)
- 학습관리 재설계: `/today`(오늘 할 일) 신규 화면 — 오늘 콤보 진행률(attemptId 기반 정확 집계), 이번 주 과제 자기신고 체크박스, D-day 연동 회피 감지. 홈/학습분석에 코칭 티저 위젯 추가 (2026-07-15)
- Groq에서 내려간 `llama-4-scout-17b-16e-instruct`(404) → `llama-3.3-70b-versatile`로 교체. 태깅 콜(`extractFeedbackTags`)은 채점보다 가벼운 닫힌 분류 작업이라 `llama-3.1-8b-instant`로 분리해 무거운 모델의 일일 토큰 한도를 아낌 (2026-07-31)
- `docs/codebase-risk-audit-2026-08-13.md` / `docs/product-contract-audit-2026-08-13.md` 1단계(데모를 막는 확정 기능 오류) 8건 수정 + 회귀 테스트 추가 (2026-08-13)
  - PC-01/CORE-01: 모의고사 자기소개(questionType=null) 답변 시 NPE로 finalize가 막히던 문제. 자기소개는 실제 시험에서도 채점 문항으로 취급되지 않으므로 채점/태깅 LLM 호출 없이 완료 처리하고 DB에도 저장하지 않음(FeedbackResult 미생성) — "총 문항 수"/"최근 기록"/"코칭 열람 조건" 등 문항 개수 기반 통계에 섞이지 않도록 함. `typeLabel(null)` null-safe 처리
  - PC-02/DOMAIN-01: "돌발로 하기"가 일반 배경설문 주제까지 섞어 내던 문제 → `TopicCatalog.surpriseTopics()` 전용 풀만 사용
  - PC-03: 유형별 연습이 전체 지원 주제에서 뽑던 문제 → 사용자가 선택한 배경설문 주제 교집합에서만 출제
  - PC-04: 난이도에 없는 C4/C5 category 요청 시 다른 category로 조용히 대체되던 문제 → 명시적 실패로 전환. 1차 수정에선 백엔드만 막고 화면은 그대로 둬서 "버튼 눌러도 설명 없이 홈으로 튕기는" 상태였는데, 후속 수정으로 `/practice/focus`(집중 연습 콤보별 탭)와 `/exam`(시험 계획 약한 콤보 추천)이 현재 난이도에서 실제로 지원하는 category만 보여주도록 필터링
  - PC-05: 마이페이지 저장 시 거주 주제(LIVING_WITH_FAMILY/ALONE)가 매번 삭제되던 문제 → 저장 시 현재 거주 형태에 맞는 주제로 재계산
  - PC-10: 현재 문항 녹음이 "다음"을 누르기 전까지는 이탈 경고 대상이 아니었던 문제 수정
  - PC-11: 마이페이지의 "12개 이상 + 그룹별 최소" 규칙이 서버에 없어 0개 저장/toggle로 회피 가능했던 문제 → `SurveyTopicPolicy`로 온보딩과 마이페이지가 같은 규칙 공유. 1차 수정에선 서버 검증만 추가하고 화면엔 실패 이유를 안 보여줘서 "저장 버튼 눌러도 조용히 실패하는" 상태였는데, 후속 수정으로 마이페이지/온보딩 주제선택 화면에 `?error=invalidTopics` 배너 추가
  - PC-21: 온보딩을 두 탭에서 완료하면 두 번째 요청이 500이 나던 문제 → 기존 profile 존재 시 멱등 리다이렉트 + unique 제약 위반 방어
- `docs/codebase-risk-audit-2026-08-13.md` 중 준비도 READY이고 범위가 작은 5건 수정 + 회귀 테스트 추가 (2026-08-13)
  - ADMIN-01: `/admin/question-sets`가 `QuestionSet`에 없는 `difficulty` 필드를 템플릿에서 참조해 세트가 1개라도 있으면 500이 나던 문제 → 난이도 컬럼 자체 제거(QuestionSet은 애초에 난이도 개념이 없음)
  - SEC-03: `.dockerignore` 부재로 `.env`/`.git`이 Docker build context로 전송되던 문제 → `.dockerignore` 추가, 실제 build context에 두 파일이 안 들어가는 것을 더미 Dockerfile로 직접 검증
  - SCORE-01: `ExamPlanService.weightedAvg()`/`weightedAvgList()`의 지수 감쇠 가중치 방향이 반대였던 문제(최신순 리스트인데 가장 오래된 값에 가중치 1.0) → `alpha^i`로 수정해 index 0(최신)이 가장 큰 가중치를 갖도록 함
  - SCORE-02: TYPE_5~7(롤플레이) MP "평가 제외" 0점이 다른 4개 점수와 그대로 평균 계산에 섞여 롤플레이를 연습할수록 등급이 구조적으로 하락하던 문제 → 롤플레이 MP 점수를 0 대신 null로 저장, 기존 null-safe 평균 로직(computeGrade/computeOverallText/weightedAvg)이 자동으로 분모에서 제외
  - CACHE-01: 관리자가 질문 세트를 생성/수정/삭제해도 `QuestionAssemblyService`의 무기한 캐시(`setCache`)가 반영 안 되던 문제 → `QuestionAssemblyService.evict(topic)` 추가, `AdminQuestionSetApiController`가 CRUD 후 호출(수정 시 topic이 바뀔 수 있어 이전/이후 topic 둘 다 무효화)
- SEC-04: Tomcat CVE(CVE-2025-48988/48976/31650) 대응 → Spring Boot `3.4.4` → `3.4.13`(같은 3.4.x 라인 최신 패치), Tomcat이 `10.1.39` → `10.1.50`으로 따라 올라감. 공식 advisory 기준 세 CVE 모두 수정 버전(10.1.40/10.1.42) 이후. 전체 유닛/컨트롤러 테스트 통과 확인 (2026-08-13)
- SEC-02(부분): `application.properties.old`(로컬 DB 비밀번호 평문 + 주석 처리된 카카오 client-secret 포함, 2025-03-30 최초 커밋부터 계속 public 저장소에 존재) `git rm --cached`로 추적 제거, `.gitignore`에 `*.properties.old` 등 재발 방지 패턴 추가. 과거 Git 히스토리에는 값이 여전히 남아있고(히스토리 재작성은 이번엔 보류) 카카오 secret 재발급은 사용자가 카카오 개발자 콘솔에서 직접 처리해야 함 — 미완료 상태로 남김 (2026-08-13)
- SEC-01: `/admin/**`, `/api/admin/**`가 로그인 여부만 확인하고 실제 관리자 권한은 안 보던 문제 → `SecurityConfig`에 `.hasAuthority("ADMIN")` 추가(뷰 라우트 `/admin/**`는 기존에 matcher 자체가 없어 `anyRequest().authenticated()`로 새고 있었음). `CustomOAuth2UserService`가 이미 `Member.role`을 authority로 부여하고 있어 matcher 수정만으로 해결. `spring-security-test` 의존성 추가, 비인증/일반 USER/ADMIN 세 케이스 회귀 테스트(`SecurityConfigAdminAccessTest`) (2026-08-13)
- COST-01(부분): 비용 API rate limiter가 존재하지 않는 경로(`/practice/combo/feedback`)에만 걸려있어 실제 STT+LLM 호출 경로(`/api/practice-attempts/*/answers`, `/answers/retry`, `/analytics/coaching`)엔 아무 제한이 없던 문제 → limiter 경로 교체(`/analytics/coaching`은 GET/POST 공유 경로라 `RateLimitInterceptor`가 POST만 소비하도록 수정). 한도 단위도 "요청 수"에서 "문항 수"로 재정의(시간당 15문항, 모의고사처럼 여러 문항을 한 요청에 몰아 제출해도 실제 문항 수만큼 소비, 자기소개는 채점 대상이 아니므로 미포함). `PracticeAttemptApiController`에 요청 증폭 방어 추가: questionIndexes null·중복 거부, 문항 수 초과 거부, 이미 성공한 문항 재제출 거부, 답변 파일 15MB 초과·`audio/webm` 외 형식 거부. 비로그인 사용자는 연습 시작 라우트가 전부 로그인 필수라 이 경로에 실질적으로 도달 불가함을 확인(anonymous 공유 버킷은 죽은 방어선). IP/member/전역 provider quota 같은 예산 정책은 제품 결정이 필요해 미완료로 남김. 테스트: `PracticeAttemptApiControllerCostGuardTest`, `RateLimitInterceptorTest` (2026-08-13)
- COST-01 후속 수정: rate limit 소비 시점이 컨트롤러 검증보다 먼저(`RateLimitInterceptor`)라, 중복 index·대용량 파일 등으로 결국 400 거부될 요청도 한도를 미리 깎아먹는 문제를 리뷰로 발견 → 버킷 로직을 `RateLimiterService`로 분리, `/api/practice-attempts/*/answers[/retry]`는 인터셉터에서 소비하지 않고 `PracticeAttemptApiController`가 모든 검증 통과 후 LLM 호출 직전에 직접 소비하도록 변경(검증 실패 = 한도 소비 없음). `/analytics/coaching`은 검증이 단순해 인터셉터 그대로 유지. `RateLimitExceededException` 추가(429) (2026-08-13)
- SEC-01 운영 후속 조치 필요 기록: 저장소에 `Role.ADMIN`을 실제로 부여하는 코드 경로가 없어(`CustomOAuth2UserService`는 항상 `Role.USER`로 생성), SEC-01 배포 시 DB에서 수동으로 role을 ADMIN으로 바꾸기 전까지 아무도 `/admin`에 접근할 수 없음을 확인 — 코드 수정 없이 감사 문서에만 기록 (2026-08-13)
- PC-11 재검토 수정: 마이페이지 주제 토글 API가 "제거"할 때는 최소 개수를 검증하면서 "추가"할 때는 허용 목록(돌발 전용 주제 등) 검증이 빠져있어, 화면 밖에서 API를 직접 호출하면 돌발 전용 주제를 개인 주제로 넣을 수 있던 문제 → `SurveyTopicPolicy.isAllowedTopic()` 추가, `MyPageController.toggleTopic()` 추가 경로에 적용. 회귀 테스트 추가 (2026-08-13)
- SEC-05: Grafana `GF_SECURITY_ADMIN_PASSWORD`가 `GRAFANA_PASSWORD` 없으면 `admin`으로 조용히 fallback하던 것 → `${GRAFANA_PASSWORD:?...}`로 변경해 값 없으면 compose가 즉시 실패하도록 수정 (2026-08-13)
- SEC-07: `/actuator/**`(prometheus/health 등)가 nginx를 통해 외부에 무인증으로 노출되던 문제 → `nginx.conf.template`에 `/actuator/` 차단 location 추가. 내부 Prometheus는 nginx를 거치지 않고 Docker network로 직접 scrape하므로 영향 없음 (2026-08-13)
- SEC-06: OAuth 세션 앱인데 CSRF가 전역 비활성화돼있던 문제 → `SecurityConfig`에서 CSRF 비활성화 제거(기본 설정 유지). Thymeleaf `th:action` 폼은 자동으로 토큰이 실림. JS `fetch()`로 상태를 바꾸는 4개 템플릿(관리자 CRUD, 마이페이지/온보딩 주제 토글, 답변 제출/finalize)엔 `<meta name="_csrf">` 추가 후 각 fetch 헤더에 토큰 첨부. 회귀 테스트: "ADMIN 권한이어도 CSRF 없으면 403" 케이스로 권한 체크와 CSRF가 별개 관문임을 증명 (2026-08-13)
- DATA-01(단일 인스턴스 전제): 동시에 finalize 요청이 두 번 들어오면(더블클릭, 두 탭) 둘 다 attempt가 아직 `IN_PROGRESS`인 걸 보고 통과해 피드백이 중복 저장될 수 있던 문제 → `CaffeinePracticeAttemptStore.tryMarkSubmitted()`를 `cache.asMap().computeIfPresent()`로 구현해 상태 전이 자체를 원자적으로 만들고, `PracticeAttemptApiController.finalize()`가 DB 저장 전에 이 전이부터 확정(실패하면 저장 없이 즉시 예외). feedback/tag 저장은 새 `@Service` `FeedbackPersistenceService`로 분리해 하나의 `@Transactional`로 묶음(컨트롤러 self-invocation으론 `@Transactional`이 프록시를 안 거쳐 무시되던 걸 같이 해결). Caffeine은 JVM 로컬 메모리라 이 원자성은 단일 인스턴스 배포에서만 보장 — 다중 인스턴스로 확장 시 Redis 등 외부 store로 교체 필요. 세션 결과 map 경합(별도 탭)은 이번 범위 밖. 테스트: `CaffeinePracticeAttemptStoreTest`(스레드 50개 동시 호출로 정확히 1개만 성공함을 실증), `PracticeAttemptApiControllerFinalizeTest`, `FeedbackPersistenceServiceTest` (2026-08-18)
